#include "monty.h"
#include <stdlib.h>
#include <stdio.h>

int main(int ac , char **av)
{
	stack_t *mem;
	mem = NULL;
	if (ac!= 2)
	{
		fprintf(stderr, "USAGE: monty file \n");
		exit(EXIT_FAILURE);
	}
}

